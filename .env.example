# Google Sheets Integration (optional — app works with CSVs out of the box)
# ------------------------------------------------------------------
# 1. Go to console.cloud.google.com → APIs & Services → Credentials
# 2. Create OAuth 2.0 Client ID → Application type: "Desktop app"
# 3. Download the JSON → save as "credentials.json" in project root
# 4. Enable "Google Sheets API" and "Google Drive API"
# 5. Set the Sheet ID below (from the spreadsheet URL)
# 6. The first time you run the app, a browser opens for Google login
#    → token is saved as authorized_user.json (auto-refreshed)

# Google Sheet ID (from the spreadsheet URL)
# https://docs.google.com/spreadsheets/d/<THIS_IS_THE_SHEET_ID>/edit
GOOGLE_SHEET_ID=your-google-sheet-id-here

# Google Gemini LLM (optional — enables AI-powered natural language responses)
# Get a free API key from https://aistudio.google.com
GEMINI_API_KEY=your-gemini-api-key-here
